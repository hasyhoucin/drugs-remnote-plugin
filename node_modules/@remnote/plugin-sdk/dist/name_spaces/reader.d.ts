import { NamespaceBase, InternalCallMethod } from './base';
import { Rem } from './rem';
/**
 * # Reader Namespace
 */
export declare class ReaderNamespace extends NamespaceBase {
    /** @hidden */
    constructor(_call: InternalCallMethod);
    /**
     * Helper method for methods that return a single rem (`RemPluginObject`)
     */
    private callAndReturnRem;
    /**
     * Add a highlight to the active reader.
     * @returns The newly created highlight.
     */
    addHighlight(): Promise<Rem | undefined>;
}
